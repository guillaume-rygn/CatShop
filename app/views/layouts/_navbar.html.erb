
<header class="p-3 bg-nav ">
  <div class="container">
    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">

      <a href="<%=root_path%>" class="text-center mb-md-2 mb-sm-2 col-md-12 col-sm-12 col-lg-1">
        <img src="paw.png" alt=""  width="50" height="50" >
      </a>

      <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
      </ul>

      <div class="text-end">


         <%  if user_signed_in? %>
            <%= link_to "My order", orders_path  , class:"btn btn-nav me-2"%> <!-- order_path(order.id)-->
            <%= link_to "My profil", user_path(current_user.id)  , class:"btn btn-nav me-2"%> <!-- cart_path -->
            <a href="<%= cart_path(current_user.id) %>" class="btn btn-nav me-2"><img class="logocart" src="https://www.fisiocrem.com/wp-content/uploads/2020/06/cart-icon-300x300.png" alt="" width="30" height="30"></a>
            <%= link_to "Log-out", destroy_user_session_path , method: :delete, class:"btn btn-signup"%>
          <%else%>

            <%= link_to "Log-in", new_user_session_path , class:"btn btn-nav me-2"%>
            <%= link_to "Sign-up", new_user_registration_path , class:"btn btn-signup"%>

        <% end %>
      </div>
    </div>
  </div>
</header>
